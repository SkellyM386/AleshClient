module.exports = {
  name: "ping",
  execute({
    client: client,
    message: message,
    args: args,
    command: command,
    Discord: Discord,
  }){
  message.channels.send('ping')
  .then(m => {
  m.edit(`pong: ${message.createdTimestamp - m.createdTimestamp}`);
  });

  }


}
